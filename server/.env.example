# Clerk (get from https://dashboard.clerk.com/last-active?path=api-keys)
CLERK_JWT_ISSUER=https://<your-instance>.clerk.accounts.dev

# Resend (https://resend.com/webhooks — used by the Svix-signed /api/resend-webhook handler)
RESEND_WEBHOOK_SECRET=whsec_...

# Postgres orders service (stubbed; replace with your own)
ORDERS_DB_URL=http://localhost:5430

# NOT WIRED YET — the agent will add these once AgentMail integration lands.
# AGENTMAIL_API_KEY=...
# AGENTMAIL_WEBHOOK_SECRET=whsec_...
